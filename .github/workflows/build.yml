name: Build CSPB APK

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Debug Folder Structure
        run: |
          mkdir extracted_project
          unzip PB.zip -d extracted_project
          echo "--- DAFTAR SEMUA FILE ---"
          find extracted_project -maxdepth 2
          
